import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // 60% Dominant surfaces/backgrounds
  static const Color canvas = Color(0xFFF4F7FB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color softPanel = Color(0xFFEAF0FF);

  // 30% Primary identity
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryDeep = Color(0xFF1E40AF);
  static const Color primaryMuted = Color(0xFFDBEAFE);

  // 10% Accent energy
  static const Color accent = Color(0xFFFF8A3D);
  static const Color accentSoft = Color(0xFFFFE8DA);

  // Semantic / typography
  static const Color success = Color(0xFF0F9D73);
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color divider = Color(0xFFD6DEEA);
}
